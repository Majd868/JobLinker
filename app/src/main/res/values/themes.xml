<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base Application Theme -->
    <style name="Theme.JobLinker" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/text_white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_dark</item>
        <item name="colorOnSecondary">@color/text_white</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/background</item>

        <!-- Background colors -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_primary</item>

        <!-- Error color -->
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/text_white</item>

        <!-- Widget styles -->
        <item name="textInputStyle">@style/Widget.JobLinker.TextInputLayout</item>
        <item name="materialButtonStyle">@style/Widget.JobLinker.Button.Primary</item>
        <item name="materialCardViewStyle">@style/Widget.JobLinker.Card</item>

        <!-- Text appearances -->
        <item name="textAppearanceHeadline1">@style/TextAppearance.JobLinker.HeadlineLarge</item>
        <item name="textAppearanceHeadline2">@style/TextAppearance.JobLinker.HeadlineMedium</item>
        <item name="textAppearanceBody1">@style/TextAppearance.JobLinker.BodyLarge</item>
        <item name="textAppearanceBody2">@style/TextAppearance.JobLinker.BodyMedium</item>

        <!-- Action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Splash Theme -->
    <style name="Theme.JobLinker.Splash" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@color/primary</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/primary</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Full Screen Theme (for CallActivity) -->
    <style name="Theme.JobLinker.FullScreen" parent="Theme.JobLinker">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>